# Sui Network Configuration
# Network: mainnet or testnet
NETWORK=mainnet

# Sui RPC endpoint (leave empty to use default)
# Default mainnet: https://fullnode.mainnet.sui.io:443
# Default testnet: https://fullnode.testnet.sui.io:443
# Alternative RPC endpoints:
#   - https://sui-mainnet.gateway.tatum.io
#   - https://sui-mainnet-endpoint.blockvision.org/
SUI_RPC_URL=

# Wallet Configuration
# Your Sui wallet private key (KEEP THIS SECURE!)
# Must be exactly 64 hexadecimal characters (or 66 with 0x prefix)
# NEVER commit this to git or share it publicly
PRIVATE_KEY=

# Bot Configuration
# Check interval in seconds (how often to check positions)
# Recommended: 300 (5 minutes) for production, 60 for testing
CHECK_INTERVAL=300

# Rebalance threshold (percentage, e.g., 0.05 = 5%)
# Bot will rebalance when price moves this % outside the range
REBALANCE_THRESHOLD=0.05

# Pool Configuration
# Pool address to manage (must be a valid Cetus CLMM pool)
# Find pools at: https://app.cetus.zone/
# Example: 0xf699e7f2276f5c9a75944b37a0c5b5d9ddfd2471bf6242483b03ab2887d198d0
POOL_ADDRESS=

# Position to track (optional but recommended)
# When set, the bot tracks and rebalances ONLY this specific position.
# The rebalanced position will use exactly the same liquidity amounts
# as the original — no extra funds are pulled from the wallet.
# Set this to the position object ID shown at startup or in your wallet.
POSITION_ID=

# Position Configuration
# Minimum position tick index (lower price bound)
LOWER_TICK=

# Maximum position tick index (upper price bound)
UPPER_TICK=

# Range width in ticks (optional, used for auto-calculation)
# When not set, the bot uses the tightest active range (1 tick spacing)
# to maximise capital efficiency.  Set a larger value (e.g. tick_spacing * 10)
# for a wider, more stable range.
RANGE_WIDTH=

# Liquidity amount to manage (in token amounts)
# If not set, bot will use 10% of available token balance
TOKEN_A_AMOUNT=
TOKEN_B_AMOUNT=

# Risk Management
# Maximum slippage tolerance (percentage, e.g., 0.01 = 1%)
MAX_SLIPPAGE=0.01

# Gas budget for transactions (in MIST, 1 SUI = 1,000,000,000 MIST)
# Reduced default to lower network fees while remaining sufficient for CLMM operations
GAS_BUDGET=50000000

# Logging
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Enable detailed transaction logging
VERBOSE_LOGS=false

# Dry Run Mode
# Set to 'true' to simulate transactions without executing them
# Useful for testing configuration and strategy
# IMPORTANT: Always test with DRY_RUN=true first!
DRY_RUN=false
